services:
  - type: web
    name: surprise-granite
    runtime: static
    buildCommand: echo "Static site ready"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
