# robots.txt for newcountertops.com
#
# Without this file Cloudflare serves a generated one, which is not ours and
# names no sitemap — so nothing here tells a crawler what exists.
#
# No per-bot groups. A named "User-agent: X" group REPLACES the "*" group for
# that crawler rather than adding to it (RFC 9309), which is how a site ends up
# with rules that silently apply to nobody. One group, everyone.

User-agent: *
Allow: /

# Nothing to hide yet. When there is — thank-you pages, anything behind a form —
# it goes here, not in a meta tag on a page a crawler has to fetch first.

Sitemap: https://newcountertops.com/sitemap.xml
